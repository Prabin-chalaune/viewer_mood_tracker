services:
  - type: web
    name: viewer-mood-tracker
    env: python
    pythonVersion: 3.9.18
    buildCommand: |
      python -m pip install --upgrade pip setuptools wheel
      pip install -r requirements.txt
    startCommand: python app.py
